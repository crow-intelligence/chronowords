import doctest

import numpy as np

from src.chronowords.algebra.svd import (AnalogyResult, SVDAlgebra,
                                         WordSimilarity)

# Run doctests
doctest.testmod(verbose=True)
